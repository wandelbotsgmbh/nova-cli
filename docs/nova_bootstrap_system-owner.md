## nova bootstrap system-owner

Write the system-owner Secret manifest

### Synopsis

Applies a Kubernetes Secret containing the authenticated user's email
and refresh token to the cluster via kubectl.

The Service Manager Backend reads this secret at runtime for:
  - Unattended communication with the developer portal API
  - License activation via Cryptlex

Requires prior authentication via "nova login" and a configured kubectl context.

```
nova bootstrap system-owner [flags]
```

### Options

```
  -h, --help   help for system-owner
```

### Options inherited from parent commands

```
      --dry-run            Print manifests to stdout instead of applying via kubectl
      --namespace string   Target Kubernetes namespace (default "wandelbots")
```

### SEE ALSO

* [nova bootstrap](nova_bootstrap.md)	 - Bootstrap NOVA on an on-premise IPC

###### Auto generated by spf13/cobra on 15-May-2026
