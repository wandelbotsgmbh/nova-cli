## nova bootstrap reflector

Deploy Ember Stack Reflector for secret mirroring

### Synopsis

Applies a HelmChart CR that deploys Ember Stack Reflector.

Reflector automatically mirrors Secrets and ConfigMaps across namespaces
based on annotations. It is required so that pull secrets created in
kube-system are available in all other namespaces.

Requires a configured kubectl context.

```
nova bootstrap reflector [flags]
```

### Options

```
  -h, --help   help for reflector
```

### Options inherited from parent commands

```
      --dry-run            Print manifests to stdout instead of applying via kubectl
      --namespace string   Target Kubernetes namespace (default "wandelbots")
```

### SEE ALSO

* [nova bootstrap](nova_bootstrap.md)	 - Bootstrap NOVA on an on-premise IPC

###### Auto generated by spf13/cobra on 15-May-2026
