## nova nucleus

EXPERIMENTAL: Interact with an Omniverse Nucleus server

### Synopsis

EXPERIMENTAL: Commands for listing, copying, and removing files on an Omniverse Nucleus server.

Configuration:
  Before using nucleus commands you need to configure the server host and an API token.

  # Set the Nucleus server host (persisted in config)
  nova config set nucleus-host nucleus.dev.wandelbots.io

  # Set the API token (persisted in config)
  nova config set nucleus-token <your-token>

  Alternatively, use flags (--nucleus-host, --nucleus-token) or the NUCLEUS_TOKEN
  environment variable for per-invocation overrides. TLS is enabled by default and
  can be disabled with --nucleus-tls=false.

  Verify your configuration with:
  nova config view

Usage examples:
  nova nucleus ls /
  nova nucleus ls -l /Projects/
  nova nucleus cp ./Scene.usd n:/Projects/Scene.usd
  nova nucleus cp n:/Projects/Scene.usd ./local.usd
  nova nucleus mv /Projects/Old.usd /Projects/New.usd
  nova nucleus rm /Projects/Unused.usd
  nova nucleus mkdir /Projects/NewFolder/
  nova nucleus stat /Projects/Scene.usd

### Options

```
  -h, --help                   help for nucleus
      --nucleus-host string    Nucleus server host (overrides config)
      --nucleus-tls            Use TLS (wss://) (default true)
      --nucleus-token string   Nucleus API token (overrides NUCLEUS_TOKEN env)
```

### SEE ALSO

* [nova](nova.md)	 - CLI for interacting with Wandelbots Nova
* [nova nucleus cp](nova_nucleus_cp.md)	 - Copy files between local filesystem and Nucleus
* [nova nucleus ls](nova_nucleus_ls.md)	 - List files and directories
* [nova nucleus mkdir](nova_nucleus_mkdir.md)	 - Create a directory
* [nova nucleus mv](nova_nucleus_mv.md)	 - Move or rename a file or directory
* [nova nucleus rm](nova_nucleus_rm.md)	 - Remove a file or directory
* [nova nucleus stat](nova_nucleus_stat.md)	 - Show details about a file or directory

###### Auto generated by spf13/cobra on 24-Apr-2026
