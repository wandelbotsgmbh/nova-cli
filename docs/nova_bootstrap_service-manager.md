## nova bootstrap service-manager

Write the service-manager HelmChart manifest

### Synopsis

Applies a HelmChart custom resource to the cluster via kubectl.

k3s deploys HelmChart CRs via its built-in Helm controller.

By default uses version "x.x.x" which resolves to the latest available release.

```
nova bootstrap service-manager [flags]
```

### Options

```
  -h, --help                  help for service-manager
      --oci-chart string      Chart path within the OCI registry (default "helm/service-manager")
      --oci-registry string   OCI registry for the chart (default "wandelbots.azurecr.io")
      --pull-secret string    Docker registry secret name for chart pulls (default "pull-secret-wandelbots-azurecr-io")
      --version string        Service-manager chart version (default uses latest) (default "x.x.x")
```

### Options inherited from parent commands

```
      --dry-run            Print manifests to stdout instead of applying via kubectl
      --namespace string   Target Kubernetes namespace (default "wandelbots")
```

### SEE ALSO

* [nova bootstrap](nova_bootstrap.md)	 - Bootstrap NOVA on an on-premise IPC

###### Auto generated by spf13/cobra on 15-May-2026
