## nova model build

EXPERIMENTAL: Build and push a model image

### Synopsis

EXPERIMENTAL: Builds a multi-platform model image (linux/amd64, linux/arm64) using the Dockerfile
and model.yaml created by 'nova model init', and pushes it to the configured registry.

The path should point to a directory containing a Dockerfile and model.yaml
(as created by 'nova model init').

Use --tag to specify the full image tag (e.g. myregistry.io/models/llama:1.0.0).

Examples:
  nova model build ./llama-2-7b --tag myregistry.io/models/llama:1.0.0
  nova model build . --tag ghcr.io/org/my-model:latest

```
nova model build [path] [flags]
```

### Options

```
  -h, --help         help for build
  -t, --tag string   Full image tag including registry (e.g. myregistry.io/models/llama:1.0.0)
```

### SEE ALSO

* [nova model](nova_model.md)	 - EXPERIMENTAL: Build and push model images

###### Auto generated by spf13/cobra on 15-May-2026
