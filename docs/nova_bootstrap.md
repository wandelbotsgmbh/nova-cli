## nova bootstrap

Bootstrap NOVA on an on-premise IPC

### Synopsis

Bootstrap a NOVA installation on an on-premise IPC.

Prerequisites:
  - Run "nova login" first to authenticate with the developer portal.
  - kubectl must be configured to access the target cluster e.g. "kubectl config use-context" or
  "export KUBECONFIG=path/to/config" 

The bootstrap process:
  1. Creates the target namespace (nova bootstrap namespace)
  2. Deploys Ember Stack Reflector for cross-namespace secret mirroring (nova bootstrap reflector)
  3. Applies pull secrets from the developer portal (nova bootstrap pull-secrets)
  4. Applies the service-manager HelmChart CR (nova bootstrap service-manager)
  5. Applies the system-owner Secret (nova bootstrap system-owner)

All manifests are applied to the cluster via "kubectl apply".

Use --dry-run to print manifests to stdout instead of applying them.
Use "nova bootstrap all" to run all steps in sequence.

### Examples

```
 # point kubectl to the target cluster context
 export KUBECONFIG=~/.kube/gpu-workstation

 # login to the developer portal
 nova login

 # bootstrap the nova
 nova bootstrap all

```

### Options

```
      --dry-run            Print manifests to stdout instead of applying via kubectl
  -h, --help               help for bootstrap
      --namespace string   Target Kubernetes namespace (default "wandelbots")
```

### SEE ALSO

* [nova](nova.md)	 - CLI for interacting with Wandelbots Nova
* [nova bootstrap all](nova_bootstrap_all.md)	 - Run all bootstrap steps in sequence
* [nova bootstrap namespace](nova_bootstrap_namespace.md)	 - Create the target namespace
* [nova bootstrap pull-secrets](nova_bootstrap_pull-secrets.md)	 - Write container registry pull secrets
* [nova bootstrap reflector](nova_bootstrap_reflector.md)	 - Deploy Ember Stack Reflector for secret mirroring
* [nova bootstrap service-manager](nova_bootstrap_service-manager.md)	 - Write the service-manager HelmChart manifest
* [nova bootstrap system-owner](nova_bootstrap_system-owner.md)	 - Write the system-owner Secret manifest

###### Auto generated by spf13/cobra on 15-May-2026
