## nova bootstrap pull-secrets

Write container registry pull secrets

### Synopsis

Fetches image-registry credentials from the developer portal API and
applies them as Kubernetes Secrets via kubectl.

The secrets include Reflector annotations so they are automatically mirrored
to all namespaces.

Requires prior authentication via "nova login" and a configured kubectl context.

```
nova bootstrap pull-secrets [flags]
```

### Options

```
  -h, --help   help for pull-secrets
```

### Options inherited from parent commands

```
      --dry-run            Print manifests to stdout instead of applying via kubectl
      --namespace string   Target Kubernetes namespace (default "wandelbots")
```

### SEE ALSO

* [nova bootstrap](nova_bootstrap.md)	 - Bootstrap NOVA on an on-premise IPC

###### Auto generated by spf13/cobra on 15-May-2026
