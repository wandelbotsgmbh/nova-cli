## nova nucleus rm

Remove a file or directory

### Synopsis

Remove a file or directory on the Nucleus server. Use -r to remove non-empty directories.

```
nova nucleus rm <path> [flags]
```

### Examples

```
  nova nucleus rm /Projects/OldFile.usd
  nova nucleus rm -r /Projects/OldDir/
```

### Options

```
  -h, --help        help for rm
  -r, --recursive   Recursively delete non-empty directories
```

### Options inherited from parent commands

```
      --nucleus-host string    Nucleus server host (overrides config)
      --nucleus-tls            Use TLS (wss://) (default true)
      --nucleus-token string   Nucleus API token (overrides NUCLEUS_TOKEN env)
```

### SEE ALSO

* [nova nucleus](nova_nucleus.md)	 - EXPERIMENTAL: Interact with an Omniverse Nucleus server

###### Auto generated by spf13/cobra on 24-Apr-2026
